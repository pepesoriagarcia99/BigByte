import { API_ROOT, IP, PORT, PROTOCOL } from "../constant";

export default {
    DEFAULT_ENV_VALUES: {
        [PROTOCOL]: 'http',
        [IP]: '0.0.0.0',
        [PORT]: 8080,
        [API_ROOT]: '/api',
    },
    INHERITED_ARGV: [
        
    ]
}